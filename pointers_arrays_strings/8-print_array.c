#include "main.h"
#include <stdio.h>
/**
 * print_array - check the code for
 *
 * @n : array.
 * @a : arraya.
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	for (n = 0; n < 5; n++)
	{
		printf(" %d\n", a[n]);
	}
}

