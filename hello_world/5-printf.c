#include <stdio.o>

/**
 * main - prints "with proper grammar, but the outcome is 
 * a piace of art," followed by newline
 * Return: Always 0.
 *
 */
int main(void) {
       printf("with proper grammar, but the outcome is a piece of art,/n")
       return (0);
}       
